platforms:
  ios:
    targets:
      App:
        entitlements:
          - aps-environment: production
        plist:
          replace: false
          entries:
            - UIBackgroundModes:
              - remote-notification